version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -e .
run:
  command: uvicorn synapse_dicom_viewer.proxy:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
    env: APP_PORT
  env:
    - name: HOSTED
      value: "1"
